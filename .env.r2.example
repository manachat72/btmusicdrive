# Cloudflare R2 — คัดลอกเป็น .env.r2 แล้วใส่ค่าจริง (ไฟล์ .env.r2 ถูก gitignore ไว้แล้ว)
# หาได้ที่ Cloudflare Dashboard → R2 → Manage R2 API Tokens → Create API Token (Object Read & Write)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=btmusicdrive-img
